{
  "entities": {
    "message": {
      "title": "Message",
      "description": "A feedback message from a user.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the user sending the message."
        },
        "message": {
          "type": "string",
          "description": "The content of the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the message was created."
        }
      },
      "required": ["name", "message", "createdAt"]
    },
    "anonymousMessage": {
      "title": "Anonymous Message",
      "description": "An anonymous message from a user.",
      "type": "object",
      "properties": {
        "anonymousId": {
          "type": "string",
          "description": "The anonymous ID of the user."
        },
        "message": {
          "type": "string",
          "description": "The content of the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the message was created."
        }
      },
      "required": ["anonymousId", "message", "createdAt"]
    },
    "vipMessage": {
      "title": "VIP Message",
      "description": "A message in the VIP chat room.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user ID of the sender."
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the sender."
        },
        "photoURL": {
          "type": "string",
          "description": "The photo URL of the sender."
        },
        "message": {
          "type": "string",
          "description": "The content of the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the message was created."
        }
      },
      "required": ["uid", "displayName", "message", "createdAt"]
    },
    "userProfile": {
      "title": "User Profile",
      "description": "Stores user profile information including approval status.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID from Firebase Authentication."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "approvalStatus": {
          "type": "string",
          "enum": ["pending", "approved", "rejected"],
          "description": "The user's account approval status."
        }
      },
      "required": ["uid", "email", "approvalStatus"]
    }
  },
  "auth": {
    "providers": ["Email"]
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": { "$ref": "#/entities/message" },
      "description": "Collection of feedback messages."
    },
    "/anonymous_messages/{messageId}": {
      "schema": { "$ref": "#/entities/anonymousMessage" },
      "description": "Collection of anonymous chat messages."
    },
    "/vip_messages/{messageId}": {
      "schema": { "$ref": "#/entities/vipMessage" },
      "description": "Collection of VIP chat messages for authenticated users."
    },
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/userProfile" },
      "description": "Collection of user profiles for approval status."
    }
  }
}
