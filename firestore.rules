rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to messages, but no writes
    match /messages/{messageId} {
      allow read: if true;
      allow write: if false; // Initially locked down
    }
  }
}
